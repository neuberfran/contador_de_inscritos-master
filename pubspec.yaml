name: contador_de_inscritos_flutter
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^0.12.0+2
  flutter_bloc: ^0.15.1
  equatable: ^0.2.6
  cached_network_image: ^2.0.0-rc
  pull_to_refresh: ^1.4.5

  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/images/